import React from "react";
import { Icon } from "antd";

const Index = () => (
  <React.Fragment>
    <Icon
      type="car"
      style={{
        margin: 16,
        textAlign: "center",
        fontSize: 32,
        color: "#fff"
      }}
    />
  </React.Fragment>
);

export default Index;
